specVersion: "v1beta"
serviceId: "sv_cardiac_backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sv_cardiac_backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
